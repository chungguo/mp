<template>
  <!-- 合作医院列表页面 -->
  <t-navbar title="合作医院" left-arrow bind:go-back="goBack"></t-navbar>

  <scroll-view scroll-y="{{ true }}" class="relative z-2 box-border p-[32rpx]">
    <!-- 医院列表 -->
    <block wx:for="{{ hospitals }}" wx:key="id">
      <view class="flex flex-col rounded-[24rpx] bg-white mb-[24rpx] overflow-hidden" bindtap="navigateToHospital"
        data-id="{{ item.id }}">
        <!-- 医院图片 -->
        <t-image src="{{ item.images[0] }}" mode="aspectFill" width="100%" height="160" />
        <!-- 医院信息 -->
        <view class="flex-1 flex flex-col gap-[16rpx] px-[24rpx] py-[32rpx]">
          <view class="flex items-center gap-[12rpx]">
            <text class="text-[36rpx] font-[600] text-[#333] flex-[1]">{{ item.name }}</text>
          </view>
          <view class="flex flex-wrap gap-[12rpx]">
            <block wx:for="{{ item.tags }}" wx:key="index" wx:for-item="service">
              <view
                class="rounded-[4px] bg-[#F0FFFB] border-[1px] border-[#00D197] px-[4rpx] py-[12rpx] text-[#00D197] text-[24rpx]">
                {{ service }}</view>
            </block>
          </view>
          <view class="flex items-center gap-[8rpx] mt-[4rpx]">
            <t-icon name="location" size="24rpx" class="text-[#999]" />
            <text class="text-[24rpx] text-[#666] flex-1 truncate">{{
              item.address }}</text>
          </view>
        </view>
      </view>
    </block>
  </scroll-view>
</template>

<script lang="ts">
import { type Hospital, useHospitalStore } from '@/store/hospital';
import mpx, { createPage } from '@mpxjs/core';
import { mapActions } from '@mpxjs/pinia';

createPage({
  data: {
    hospitals: [] as Hospital[],
  },
  ...mapActions(useHospitalStore, ['fetchHospitals']),

  async onLoad() {
    mpx.showLoading({
      title: '加载中...',
      mask: true,
    });

    try {
      const hospitals = await this.fetchHospitals();
      this.setData({
        hospitals
      });
    } finally {
      mpx.hideLoading();
    }
  },

  goBack() {
    mpx.switchTab({
      url: '/pages/index',
    });
  },

  navigateToHospital(e: WechatMiniprogram.CustomEvent) {
    const { id } = e.currentTarget.dataset;
    mpx.navigateTo({
      url: `/pages/hospital/detail?id=${id}`
    });
  }
});

</script>

<style>
page {
  --td-navbar-color: white;
  --td-navbar-background: transparent;
  --td-tag-disabled-background-color: #F0FFFB;
  background-image: url("https://yjb-1376872341.cos.ap-shanghai.myqcloud.com/public/lowcode-2385148.png");
  background-repeat: no-repeat;
  background-size: 100%;
}
</style>

<script type="application/json">
{
  "navigationBarTitleText": "合作医院",
  "navigationStyle": "custom",
  "usingComponents": {
    "t-image": "tdesign-miniprogram/miniprogram_dist/image/image",
    "t-icon": "tdesign-miniprogram/miniprogram_dist/icon/icon",
    "t-navbar": "tdesign-miniprogram/miniprogram_dist/navbar/navbar"
  }
}
</script>
